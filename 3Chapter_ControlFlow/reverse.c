/* this is a example of how commas seprate operations and eval left to right */
#include <string.h>

/* reverse: reverse string s in place */
void reverse(char s[])
{
	int c, i, j; 

	for (i = 0, j = strlen(s)-1; i < j; i++, j--)
}
